JAXWS-Ex-HelloWorldClient


How to run:
* Make sure the JAXWS-Ex-HelloWorldEndpoint is deployed.
* mvn clean install
* reload maven project
* Run the Main.java class as a Java application.

Note the pom.xml that uses wsimport to generate sources. Sources are placed here:
target/generated-sources. Take a look.



